apiVersion: v1
kind: Service
metadata:
  name: {{ include "travel-planner.fullname" . }}
  labels:
    app: {{ include "travel-planner.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: {{ include "travel-planner.name" . }}
